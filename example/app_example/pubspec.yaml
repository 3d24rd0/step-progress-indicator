name: app_example
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  step_progress_indicator:
    path: ../../.

dev_dependencies:
  flutter_test:
    sdk: flutter
  coolint: 1.0.2

flutter:
  uses-material-design: true
